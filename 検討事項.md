# 検討事項

コード全体を見直して見つかった、まだ手を付けていない項目。
「実装と表示が食い違っている」もの（通知プレビュー、トライアル通知、為替、CSV の期間など）は
すでに対応済みなので、ここには残っているものだけを書いてある。

最終更新: 2026-08-10

---

## 1. オンボーディングを見たかどうかを覚えていない

**現象**
オンボーディングを飛ばす条件が「登録が1件でもあるか」だけになっている。
そのため **0件のままアプリを閉じて開き直すと、毎回3ページのオンボーディングからやり直し**になる。
そのあと空状態のガイドも出るので、案内が二重に重なる。

**なぜ問題か**
「あとで登録しよう」と思って閉じた人が、次に開くたびに同じ説明を読まされる。
初回だけ見せるつもりのものが、登録するまで毎回出る。

**関連**
- `Domain/Stores/AppStore.swift` — init 内の `if !subscriptions.isEmpty { screen = .home }`
- `Presentation/Screens/OnboardingScreen.swift`
- `Domain/Stores/AppStore.swift` — `onboardNext()`

**直すなら**
`UserDefaults` に完了フラグを1つ（例：`submemo.onboardingDone`）。
最終ページの「はじめる」とスキップの両方で立てる。init はそのフラグを見て初期画面を決める。
既存ユーザーが再びオンボーディングを見ないよう、`subscriptions` が空でない場合も立てておく。

---

## 2. 通知の予約は60件で黙って打ち切られる

**現象**
`NotificationScheduler.maxPending = 60` で、発火が近い順に60件だけ予約する。
1件のサブスクにつき最大4通（7日前・3日前・前日・当日）なので、**15件登録すると上限**に届く。
超えたぶんは何も言わずに捨てられる。

**なぜ問題か**
近い順に残す設計自体は妥当だが、ユーザーからは「登録したのに通知が来ないものがある」に見える。
iOS 側の上限（アプリあたり64件）があるので、増やして解決する話ではない。

**関連**
- `Data/Services/NotificationScheduler.swift` — `maxPending`、`apply(_:)` の `prefix(maxPending)`

**直すなら（案）**
- 上限に達していることを通知設定の画面に一言出す（「登録が多いため、直近◯件ぶんだけ予約しています」）
- または、件数が多いときは自動的にリード（何日前）を減らして件数を抑え、その旨を伝える

---

## 3. 「価格の記録」が1件しか残らない

**現象**
`Subscription.priceChange` が単数なので、2回目の値上げで前回の記録が上書きされる。
詳細画面の「価格の記録」も常に1件だけ。

**なぜ問題か**
「記録」という言葉に対して履歴になっていない。
値上げが続いたサービスほど見返す価値があるのに、直前の1回しか残らない。

**関連**
- `Domain/Models/Subscription.swift` — `priceChange: PriceChange?`
- `Domain/Stores/AppStore.swift` — `saveDraft()` 内の記録処理、`historyText(_:)`

**直すなら**
`priceChange: PriceChange?` を `priceHistory: [PriceChange]` にする。
`Subscription` は `decodeIfPresent` で前方互換を取っているので、
旧データの単数フィールドを配列1件として読み込む移行を入れれば既存データを壊さずに済む。
詳細画面は直近数件を並べる形に。

---

## 4. 組み込みのカテゴリ・支払い方法を初期状態に戻せない

**現象**
組み込みのカテゴリ（動画・音楽…）や支払い方法（メインカード…）は、
名前や色を編集すると「上書き用のカスタム項目」として保存され、元に戻す手段がない。
Debug ビルドには全削除があるが、製品ビルドには逃げ道がない。

**なぜ問題か**
名前と色は手で作り直せる（組み込みの色はすべてパレットにある）が、
**ローカライズキーが失われる**ため、以後その項目は言語を切り替えても翻訳されなくなる。

**関連**
- `Domain/Models/SubCategory.swift` / `PaymentMethod.swift` — `builtIns`
- `Domain/Stores/AppStore.swift` — `merged(builtIns:custom:hidden:)`、`saveCategory()`、`savePayment()`

**直すなら**
編集シートに「初期状態に戻す」を置き、その ID の上書きレコードを消す（＝組み込みの定義が再び効く）。
組み込みかどうかは ID で判別できるので、組み込みのときだけ出せばよい。

---

## 5. 消した組み込み支払い方法の「復活」が同期されない

**現象**
消した組み込み支払い方法は `hiddenPayments`（ID の配列）として記録し、
iCloud 合併では**和集合**を取っている。
そのため片方の端末で復活させても、もう片方の「消した」記録が勝って再び消える。

**なぜ問題か**
「消す」は同期できるのに「戻す」は同期できない、という非対称がある。
上の 4 に「初期状態に戻す」を入れると、この非対称がそのまま表に出る。

**関連**
- `Data/Services/SyncMerge.swift` — `SyncKeys.hiddenPayments` の合併
- `Data/Services/SyncTombstones.swift`

**直すなら**
サブスクやカテゴリと同じく、ID ごとに「いつ消したか／いつ戻したか」の時刻を持たせて
新しい方を採用する（tombstone と同じ考え方）。
配列ではなく `[ID: 更新時刻]` の形にすれば、復活も同期に乗る。

---

## 参考：対応済み（記録用）

- 通知プレビューが実際に届く文面と一致しない → 予約内容そのものから生成するよう変更
- 更新前の通知を全部オフにしてもトライアルが鳴る → トライアルにも「オフ」を追加、要約表示も実態に合わせた
- 為替「手動」で EUR に手が出せない → 手動レートを通貨ごとに保持、為替画面に通貨切替を追加
- 円換算の二重実装（EUR が固定 168 円） → 使われていない側を削除
- CSV の「期間」が何で絞るか不明・年がベタ書き → 「次回更新日で絞る」に変更、年を動的に
- 過去月の閲覧・前月比 → 請求記録を持たないため、UI ごと削除
- 「サブメモ プレミアム」 → 方針が決まるまで持たない方針とし、コードと文言を削除
